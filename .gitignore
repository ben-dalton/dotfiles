.DS_Store
*.bak
.config/tmux/plugins/
.config/nvim/lazy/

# Secrets — never commit (see also: showUntrackedFiles=no only hides, it does not block `add`)
.zshenv
.zshrc.local
.cloudzerorc
*keys
*.env

# Moom: layouts embed saved window titles (work email, private Slack channels,
# Jira ticket text), and ben.moomlicense is a license key. Kept local.
.moom/
